{
  "name": "gotglyphed",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build:server": "esbuild src/server.ts --bundle --platform=node --format=cjs --outfile=dist/server/index.js --external:@devvit/*",
    "build": "npm run build:server",
    "dev": "npm run build && devvit playtest",
    "upload": "npm run build && devvit upload"
  },
  "dependencies": {
    "@devvit/web": "0.13.4"
  },
  "devDependencies": {
    "@devvit/tsconfig": "0.13.4",
    "esbuild": "^0.25.0",
    "typescript": "5.7.3"
  },
  "license": "BSD-3-Clause"
}
